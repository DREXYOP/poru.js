'use strict';

const EventEmitter = require('node:events')
const discord = require("discord.js")
class __baseClient extends discord.Client{
 /**
   * The Base Client required to initilize a discord bot.
   * @extends {EventEmitter}
   */
}

module.export = __baseClient